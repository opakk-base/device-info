<Window x:Class="GetDevice.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:cvt="clr-namespace:GetDevice.Converters"
        xmlns:local="clr-namespace:GetDevice.Views"
        Title="GetDevice - Settings"
        Width="400" Height="530"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow">
    <Window.Resources>
        <cvt:BoolToGreenRedConverter x:Key="BoolToGreenRed"/>
        <local:PasswordMessageBrushConverter x:Key="BoolToErrorBrush"/>
    </Window.Resources>
    <DockPanel>
        <Grid DockPanel.Dock="Bottom" Margin="15,10,15,10" Background="Transparent">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <Ellipse Width="10" Height="10"
                         Fill="{Binding IsHttpRunning, Converter={StaticResource BoolToGreenRed}}"
                         Margin="0,0,6,0" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding HttpStatusText}" VerticalAlignment="Center"/>
            </StackPanel>
            <Button Grid.Column="1"
                    Content="{Binding HttpToggleText}"
                    Command="{Binding ToggleServerCommand}"
                    Width="90" Height="24"
                    Margin="10,0,5,0"
                    VerticalAlignment="Center"/>
            <Button Grid.Column="2"
                    Content="Close"
                    Command="{Binding CloseCommand}"
                    Width="70" Height="24"
                    Margin="5,0,0,0"
                    VerticalAlignment="Center"/>
        </Grid>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20">

            <!-- Password Change Section -->
            <TextBlock Text="Change Password" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

            <Grid Margin="0,0,0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="70"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Current:" VerticalAlignment="Center"/>
                <PasswordBox Grid.Row="0" Grid.Column="1" x:Name="CurrentPasswordBox" Height="28"/>
                <TextBox Grid.Row="0" Grid.Column="1" x:Name="CurrentPasswordTextBox"
                         Text="{Binding CurrentPassword, UpdateSourceTrigger=PropertyChanged}"
                         Height="28" Visibility="Collapsed"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="New:" VerticalAlignment="Center" Margin="0,5"/>
                <PasswordBox Grid.Row="1" Grid.Column="1" x:Name="NewPasswordBox" Height="28" Margin="0,5"/>
                <TextBox Grid.Row="1" Grid.Column="1" x:Name="NewPasswordTextBox"
                         Text="{Binding NewPassword, UpdateSourceTrigger=PropertyChanged}"
                         Height="28" Margin="0,5" Visibility="Collapsed"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Confirm:" VerticalAlignment="Center"/>
                <PasswordBox Grid.Row="2" Grid.Column="1" x:Name="ConfirmPasswordBox" Height="28"/>
                <TextBox Grid.Row="2" Grid.Column="1" x:Name="ConfirmPasswordTextBox"
                         Text="{Binding ConfirmPassword, UpdateSourceTrigger=PropertyChanged}"
                         Height="28" Visibility="Collapsed"/>
            </Grid>

            <CheckBox x:Name="ShowPasswordsCheckBox" Content="Show passwords"
                      Margin="0,5,0,0"/>

            <Button Content="Change Password"
                    Command="{Binding ChangePasswordCommand}"
                    Height="32"
                    Margin="0,5,0,10"/>

            <TextBlock Text="{Binding PasswordMessage}"
                       Foreground="{Binding PasswordMessageIsError, Converter={StaticResource BoolToErrorBrush}}"
                       Margin="0,0,0,10"
                       TextWrapping="Wrap"/>

            <Separator Margin="0,0,0,10"/>

            <!-- HTTP Server Section -->
            <TextBlock Text="HTTP Server" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

            <CheckBox Content="Enable HTTP endpoint"
                      IsChecked="{Binding IsHttpEnabled}"
                      Margin="0,0,0,5"/>

            <Grid Margin="0,0,0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Port:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBox Grid.Column="1" Text="{Binding HttpPortText, UpdateSourceTrigger=PropertyChanged}"
                         Height="28" VerticalAlignment="Center"
                         PreviewTextInput="PortTextBox_PreviewTextInput"/>
            </Grid>

            <TextBlock Foreground="Gray" FontSize="11" TextWrapping="Wrap" Margin="0,0,0,5">
                When enabled, GET /getdevice on localhost:<Run Text="{Binding HttpPortText}"/> returns filtered device info as JSON.
            </TextBlock>

            <CheckBox Content="Minimize to tray when closing window"
                      IsChecked="{Binding MinimizeToTrayOnClose}"
                      Margin="0,0,0,5"/>

            <Separator Margin="0,10"/>

            <!-- Factory Reset -->
            <TextBlock Text="Factory Reset" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
            <TextBlock Foreground="Gray" FontSize="11" TextWrapping="Wrap" Margin="0,0,0,5">
                Resets password to default '12345678'. Device ID and client key are preserved.
            </TextBlock>
            <Button Content="Reset to Default Password"
                    Command="{Binding FactoryResetCommand}"
                    Height="32"
                    Foreground="DarkRed"/>

        </StackPanel>
    </ScrollViewer>
    </DockPanel>
</Window>
