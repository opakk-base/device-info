<Window x:Class="GetDevice.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:cvt="clr-namespace:GetDevice.Converters"
        Title="GetDevice - Login"
        Width="380" Height="290"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow">
    <Window.Resources>
        <cvt:BoolToGreenRedConverter x:Key="BoolToGreenRed"/>
        <cvt:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
    </Window.Resources>
    <DockPanel Margin="20">
        <Grid DockPanel.Dock="Bottom" Margin="0,10,0,0" Background="Transparent">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <Ellipse Width="10" Height="10"
                         Fill="{Binding IsHttpRunning, Converter={StaticResource BoolToGreenRed}}"
                         Margin="0,0,6,0" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding HttpStatusText}" VerticalAlignment="Center"/>
            </StackPanel>
            <Button Grid.Column="1"
                    Content="{Binding HttpToggleText}"
                    Command="{Binding ToggleApiCommand}"
                    Width="60" Height="24"
                    VerticalAlignment="Center"/>
        </Grid>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="GetDevice" FontSize="24" FontWeight="Bold"
                       HorizontalAlignment="Center" Margin="0,0,0,10"/>

            <TextBlock Grid.Row="1" Text="Enter password to access device settings"
                       HorizontalAlignment="Center" Margin="0,0,0,15"/>

            <StackPanel Grid.Row="2" Margin="0,0,0,10">
                <TextBlock Text="Password:" Margin="0,0,0,5"/>
                <PasswordBox x:Name="PasswordBox"
                             KeyDown="PasswordBox_KeyDown"
                             Height="30"
                             FontSize="14"/>
            </StackPanel>

            <Button Grid.Row="3" Content="Unlock"
                    Command="{Binding LoginCommand}"
                    Height="35"
                    FontSize="14"
                    Margin="0,0,0,10"/>

            <TextBlock Grid.Row="4" Text="{Binding ErrorMessage}"
                        Foreground="Red"
                        HorizontalAlignment="Center"/>

            <TextBlock Grid.Row="5" Text="Forgot password? Delete appsettings.json in %LOCALAPPDATA%\GetDevice"
                        Foreground="Gray" FontSize="11"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Bottom"
                        TextWrapping="Wrap">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsFirstLaunch}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <TextBlock Grid.Row="5" Text="Hint: default password is 12345678"
                        Foreground="Orange" FontSize="11"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Bottom"
                        TextWrapping="Wrap"
                        Visibility="{Binding IsFirstLaunch, Converter={StaticResource BoolToVisibility}}"/>
        </Grid>
    </DockPanel>
</Window>
